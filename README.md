# Ohio Landcraft — Updated Brand Website

Included pages: `index.html`, `driveways.html`, `drainage.html`, `land-clearing.html`, `excavation-grading.html`, `projects.html`, `about.html`, and `evaluation.html`.

Shared files: `styles.css`, `script.js`. Brand assets are under `assets/brand/`. Add photography to `assets/images/` using the filenames in `IMAGE-LIST.md`.

The package is fully static and can be uploaded directly to a GitHub Pages repository. Before launch, add photography, replace recreated logo SVGs with original master files if available, connect the evaluation form to a real form backend/CRM, add actual contact information, analytics and Search Console, and replace example project stories with real work.
